import { TableCellSortableDirective } from './table-cell-sortable.directive';

describe('TableCellSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new TableCellSortableDirective();
    expect(directive).toBeTruthy();
  });
});
